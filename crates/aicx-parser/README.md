# aicx-parser

Pure transcript parsing, timeline typing, frontmatter extraction, segmentation,
and chunking support for `aicx`.

This crate is path-first during the migration. Full public crate documentation
lands in Phase 5.
